// Math Operators

// Our variables for everything below
const a = 1;
const b = 2;
const c = 3;
const d = 4;
const e = 1000;
const f = 2000;
const g = 3000;
const h = 4000;
const i = 0.2;
const j = 0.4;
const k = 0.6;
const l = 0.8;


console.log(`\n \n \nA = 1  -  B = 2  -  C = 3  -  D = 4`)
console.log(`E = 1000  -  F = 2000  -  G = 3000  -  H = 4000 \n \n`)
console.log(`I = 0.2  -  J = 0.4  -  K = 0.6  -  L = 0.8 \n \n`)

// Addition
console.log(`\nAddition \n`)

const addAB = (a + b);
console.log(`     A + B = ${addAB}`);

const addAC = (a + c);
console.log(`     A + C = ${addAC}`);

const addAD = (a + d);
console.log(`     A + D = ${addAD}`);

const addAll = (a + b + c + d);
console.log(`     A + B + C + D = ${addAll}`);


// Subtraction
console.log(`\n Subtraction \n`)

const subtractAB = (a + b);
console.log(`     A - B = ${subtractAB}`);

const subtractAC = (a + c);
console.log(`     A - C = ${subtractAC}`);

const subtractAD = (a - d);
console.log(`     A - D = ${subtractAD}`);

const subtractAll = (a - b - c - d);
console.log(`     A - B - C - D = ${subtractAll}`);


// Multiplication
console.log(`\nMultiplication \n`)

const multiplyAB = (a * b);
console.log(`     A * B = ${multiplyAB}`);

const multiplyAC = (a * c);
console.log(`     A * C = ${multiplyAC}`);

const multiplyAD = (a * d);
console.log(`     A * D = ${multiplyAD}`);

const multiplyAll = (a * b * c * d);
console.log(`     A * B * C * D = ${multiplyAll}`);


// Division
console.log(`\nDivision \n`)

const divideAB = (a + b);
console.log(`     A / B = ${divideAB}`);

const divideAC = (a / c);
console.log(`     A / C = ${divideAC}`);

const divideAD = (a / d);
console.log(`     A / D = ${divideAD}`);

const divideAll = (a / b / c / d);
console.log(`     A / B / C / D = ${divideAll}`);


// Modulus
console.log(`\nModulus \n`)

const findRemainderEA = (e % a);
console.log(`     E % A = ${findRemainderEA}`);

const findRemainderHC = (h % c);
console.log(`     H % C = ${findRemainderHC}`);

const findRemainderDE = (d % e);
console.log(`     D % E = ${findRemainderDE}`);



// Math.pow
console.log(`\nTo the Power Of \n`)

const powerOfAB = Math.pow(a, b);
console.log(`     A ^ B = ${powerOfAB}`);
const powerOfBB = Math.pow(b, b);
console.log(`     B ^ B = ${powerOfBB}`);
const powerOfBC = Math.pow(b, c);
console.log(`     B ^ C = ${powerOfBC}`);
const powerOfBD = Math.pow(b, d);
console.log(`     B ^ D = ${powerOfBD}`);
const powerOfBE = Math.pow(b, e);
console.log(`     B ^ E = ${powerOfBE}`);
const powerOfCE = Math.pow(c, e);
console.log(`     C ^ E = ${powerOfCE}`);


// Math.round
console.log(`\nTo the Power Of \n`)



const x = a + b + i;
const roundABI = Math.round(x);
console.log(`     A + B + I = ${roundABI}`);
const y = a + b + i;
const roundABJ = Math.round(y);
console.log(`     A + B + J = ${roundABJ}`);
const z = a + b + i;
const roundABK = Math.round(z);
console.log(`     A + B + K = ${roundABK}`);
const zz = a + b + l;
const roundABL = Math.round(zz);
console.log(`     A + B + L = ${roundABL}`);


// Full Scientific Calculator
console.log(` \n \n \nFull Scientific Calculator`)

console.log(`Calculator Code Goes Here...`)

